import pandas as pd
import cfe

# Household characteristics (d matrix)
d_panama = pd.read_csv('Panama.csv').set_index(['i','t','m'])
d_serbia = pd.read_csv('Serbia.csv').set_index(['i','t','m'])

# The expenditure data (y matrix) would need to be loaded separately —
# a DataFrame indexed by (i, t, m) with food categories as columns,
# values in log expenditures per food item.
r = cfe.Regression(y=y_panama, d=d_panama)
hello
